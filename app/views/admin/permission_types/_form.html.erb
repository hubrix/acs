<%= form_for([:admin,@resource_group,@permission_type]) do |f| %>
  <% if @permission_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@permission_type.errors.count, "error") %> prohibited this permission type from being saved:</h2>

      <ul>
      <% @permission_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit :class => 'awesome_button' %>
  </div>
<% end %>
